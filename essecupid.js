// Assume StopPropagation is defined elsewhere in your code
var next = someFunctionOrValue();

// Check if next is not equal to StopPropagation
if (next !== StopPropagation) {
    // Execute some code if the condition is true
    console.log("Next is not equal to StopPropagation.");
} else {
    // Execute alternative code if the condition is false
    console.log("Next is equal to StopPropagation.");
}
